# Verified shared training environment on this server (Python 3.14.6 / CUDA 13).
# PyTorch is inherited from the managed system environment to avoid duplicating
# several GB of CUDA wheels inside the project venv.
torch==2.13.0
numpy==2.5.1
scipy==1.17.0
transformers==5.1.0
peft==0.18.1
accelerate==1.14.0
safetensors==0.8.0
tokenizers==0.22.2
bvhio==1.5.4
hydra-core==1.3.4
omegaconf==2.3.1
einops==0.8.2
pytest==9.1.1
PyYAML==6.0.3
