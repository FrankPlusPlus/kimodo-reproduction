# Verified H200 training constraints from this server (Python 3.14.6 / CUDA 13).
# setup_env.sh consumes these pins by default. Sites with a different supported
# CUDA/PyTorch stack must provide an equally pinned file through
# KIMODO_PIP_CONSTRAINTS instead of falling back to unconstrained resolution.
torch==2.13.0
numpy==2.5.1
scipy==1.17.0
transformers==5.1.0
peft==0.18.1
accelerate==1.14.0
safetensors==0.8.0
tokenizers==0.22.2
bvhio==1.5.4
hydra-core==1.3.4
omegaconf==2.3.1
einops==0.8.2
pytest==9.1.1
PyYAML==6.0.3
